# produce a login name based on user first and last name

first = input("Enter your fist name in lowercase: ")
last = input("Enter your last name in lowercase: ")

username = first[0] + last

print(username)
